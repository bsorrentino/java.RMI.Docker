<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>rmi-labs-parent</artifactId>
        <groupId>org.bsc</groupId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>jetty-websocket</artifactId>
    <name>RMI::Websocket::Jetty::Impl</name>

    <properties>
        <jetty.version>9.4.35.v20201120</jetty.version>
        <remotehost></remotehost>
    </properties>

    <dependencies>

        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>rmi-samples</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-jdk14</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!-- To write basic websockets against -->
        <dependency>
            <groupId>org.eclipse.jetty.websocket</groupId>
            <artifactId>websocket-api</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <!-- To run websockets in embedded server -->
        <dependency>
            <groupId>org.eclipse.jetty.websocket</groupId>
            <artifactId>websocket-server</artifactId>
            <version>${jetty.version}</version>
        </dependency>
        <!-- To run websockets client -->
        <dependency>
            <groupId>org.eclipse.jetty.websocket</groupId>
            <artifactId>websocket-client</artifactId>
            <version>${jetty.version}</version>
        </dependency>
    </dependencies>

    <build>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                      <manifest>
                        <addClasspath>true</addClasspath>
                      </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>server</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>-Djava.security.policy=${project.basedir}/java.policy</argument>
                                <argument>-Djava.util.logging.config.file=${project.basedir}/logging.properties</argument>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>org.bsc.jetty_websocket.sample.rmi.SampleRemoteServer</argument>
<!--
                                <argument>org.bsc.jetty_websocket.sample.rmi.TemperatureDispatchServer</argument>
-->
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>client</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <arguments>
                                <argument>-Djava.security.policy=${project.basedir}/java.policy</argument>
                                <argument>-Djava.util.logging.config.file=${project.basedir}/logging.properties</argument>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>org.bsc.jetty_websocket.sample.rmi.SampleRemoteClient</argument>
<!--
                                <argument>org.bsc.jetty_websocket.sample.rmi.TemperatureMonitorClient</argument>
-->
                                <argument>${remotehost}</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <executable>java</executable>
                    <classpathScope>test</classpathScope>
                    <arguments>
                    </arguments>
                    <environmentVariables>
                        <RMI_HOST>localhost</RMI_HOST>
                        <RMI_PORT>1099</RMI_PORT>
                    </environmentVariables>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <profile>
            <id>docker</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>test-jar</goal>
                                </goals>
                                <configuration>
                                    <archive>
                                      <manifest>
                                        <addClasspath>true</addClasspath>
                                        <mainClass>org.bsc.jetty_websocket.sample.rmi.SampleRemoteServer</mainClass>
                                      </manifest>
                                      <manifestEntries>
                                        <Class-Path>jetty-websocket-1.0-SNAPSHOT.jar</Class-Path>
                                      </manifestEntries>
                                    </archive>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>rmiserver</id>
                                <!-- phase>package</phase -->
                                <goals>
                                    <goal>build</goal>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <images>
                                        <image>
                                            <name>rmiserver</name>
                                            <alias>dockerfile</alias>
                                            <build>
                                                <contextDir>${project.basedir}/src/main/docker</contextDir>
                                                <assembly>
                                                    <descriptor>assembly.xml</descriptor>
                                                </assembly>
                                                <dockerFile>Dockerfile</dockerFile>
                                                <filter>@</filter>
                                            </build>
                                            <run>
                                                <ports>
                                                    <port>8081:8081</port>
                                                </ports>
                                            </run>
                                        </image>
                                    </images>

                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
        </profile>

    </profiles>

</project>
